import React from 'react';
import Nav from '../Nav';




const Body=() => {
    //need to set default to render about
    const [currentPage, handlePageChange] = useState('About');

    //conditionally render nav with whichever page is selected based on state....
    return( <div></div>)
}